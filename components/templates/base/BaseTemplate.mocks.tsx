import { BaseTemplateProps } from 'components/templates/base/BaseTemplate';

const base: BaseTemplateProps = {
  sampleTextProp: 'Hello world!',
};

const mockBaseTemplateProps = {
  base,
};

export default mockBaseTemplateProps;
